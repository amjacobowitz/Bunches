import React from 'react';
import { css } from 'glamor';

import Button from '../../components/button';
import TextInput from '../../components/text-input';

import { PRIMARY, LIGHT_PRIMARY, WHITE } from '../../palette';

import fetchStudent from '../../actions/fetch-student';

export default function Landing({ children, heading, subheading }) {
  const onClick = () => {
  }

  return (
    <div { ...styles.routeContainer }>
      <TextInput placeholder='student pin' style={ styles.input }/>
      <Button text="Get Started" onClick={ onClick }/>
    </div>
  );
}

const styles = {
  routeContainer: css({
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: '10%',
    flexDirection: 'column',
  }),
  input: {
    width: '250px',
    marginBottom: '20px',
  },
}
